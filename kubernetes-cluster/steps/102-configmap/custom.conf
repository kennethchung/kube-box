server {
    listen       80;
    server_name  localhost;

    #charset koi8-r;
    #access_log  /var/log/nginx/host.access.log  main;

    location /custeromhomepage {
        root   /usr/share/nginx/json;
        index  custom.json;
    }
}